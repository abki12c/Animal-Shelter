package gr.aueb.animalshelter.domainTests;

import org.junit.jupiter.api.*;

import gr.aueb.animalshelter.domain.Account;

class AccountTest {
/*
	@Test
	void checkAccountIDincrement() {
		//start at 0/no instances created yet
		int startingCount = Account.NextAccountID();
		//create 3 Account object
		Account a1 = new Account(null);
		Account a2 = new Account(null);
		Account a3 = new Account(null);
		//get a3 ID
		int a3ID = a3.getAccountID();
		//expected a3 ID is 3
		int expID = startingCount+3;
		//check
		Assertions.assertEquals(expID, a3ID);
		
	}
*/
}
